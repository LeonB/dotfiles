"use external command because it's a LOT faster than the builtin
"vim python when starting from a sshfs directory
let g:EditorConfig_core_mode = 'external_command'
let g:EditorConfig_exec_path = '/usr/bin/editorconfig'
