; Disable menubar
(menu-bar-mode -1)

; Disable toolbar
(tool-bar-mode -1)

(require 'surround)
(global-surround-mode 1)

(load-theme 'wombat t)

;; Always enable elctrict-indent
(electric-indent-mode 1)
