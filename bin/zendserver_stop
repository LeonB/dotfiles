#!/bin/bash

shopt -s expand_aliases # for aliases to work

# RUBYLIB="$HOME/src/vagrant.git/lib/"
# alias vagrant="$HOME/src/vagrant.git/bin/vagrant"

# VBoxManage controlvm "Ubuntu 12.04: Zend Server" acpipowerbutton
# sleep 10
# VBoxManage list runningvms

cd $HOME/Workspaces/prive/vagrant-zend-server
vagrant halt $@
